# Version catalog for CloudLug.
#
# Only the JVM half of the stack from spec §3 is present. Android-side
# dependencies (AGP, Compose, Room, Hilt, AppAuth, OkHttp, ExifInterface) land in
# v0.2 — see docs/decisions.md ADR-0002 and ADR-0003.

[versions]
kotlin = "2.2.20"
coroutines = "1.10.2"
serialization = "1.8.1"
junit = "5.11.4"
junitPlatform = "1.11.4"

[libraries]
kotlinx-coroutines-core = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-core", version.ref = "coroutines" }
kotlinx-coroutines-test = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-test", version.ref = "coroutines" }
kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "serialization" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter", version.ref = "junit" }
junit-platform-launcher = { module = "org.junit.platform:junit-platform-launcher", version.ref = "junitPlatform" }

[plugins]
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
