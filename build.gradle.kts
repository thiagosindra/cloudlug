// Plugin versions live in gradle/libs.versions.toml and are applied through the
// `cloudlug.*` convention plugins in buildSrc, which put the Kotlin Gradle
// plugin on the build classpath. Declaring them again here would clash with it.
